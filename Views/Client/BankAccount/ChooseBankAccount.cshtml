@model IEnumerable<BankApp.Models.Entity.Bank>
<link rel="stylesheet" href="~/css/REGISTRUSERSTYLE.css" />
    <form asp-action="Account" method="get">
      <select  name="BankId">
        @foreach (var bnk in @Model)
         {
         <option value=@bnk.Id>@bnk.Name</option>
         }
     </select>
     <button>OK</button>
   </form>